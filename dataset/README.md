# Dataset (local only)

Video clips are stored here but are **not** pushed to GitHub (file size limits).

Place your clips under:

- `accident/` — accident clips (day/night)
- `non accident/` — normal traffic clips
- `uyir sample/` — sample videos for testing

After cloning the repo, copy your dataset into this folder or run `data_logger.py` on your own videos.
